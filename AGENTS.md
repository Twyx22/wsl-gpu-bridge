# AGENTS — wsl-gpu-bridge

Stack : python stdlib uniquement. Pas de dep sans accord explicite.
Test : `python3 -m unittest discover -s tests -v` + `python3 wsl_gpu_bridge.py detect`.
Commits : conventionnels (`feat:`, `fix:`, `docs:`). Identité : Twyx22.
Style : ponytail ultra. 1 fichier > N modules. Stdlib > dep. Supprimer > ajouter.
Ne jamais : ajouter dep, refactor large, bench sans preuve, doc verbeuse.
